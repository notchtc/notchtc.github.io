pages:
  image:
    name: klakegg/hugo:alpine
    entrypoint: [""]
  script:
    - hugo
    - gzip -k -9 $(find public -type f)
  artifacts:
    paths:
      - public
  only:
    refs:
      - master
